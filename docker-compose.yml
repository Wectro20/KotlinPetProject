version: "3.9"
services:
  mongo:
    image: mongo:4.0.4
    restart: always
    ports:
      - '27017:27017'
    volumes:
      - ./data/mongodb:/files/new_data/db

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app
    restart: on-failure
    depends_on:
      - mongo
    ports:
      - 8080:8080
    environment:
      SPRING_APPLICATION_JSON: '{
            "spring.data.mongodb.database" : "cryptocurrency-price",
            "cryptocurrency.name" : "BTC,ETH,XRP",
            "cryptocurrency.thread-sleep-time" : "5000"
          }'
